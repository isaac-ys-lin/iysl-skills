# 原型來源與適用範圍

此為校準原型，不是正式 skill，也不是公司正式 CIS。

- 指定來源：https://transglobe-blue.isaacyslin.chatgpt.site/ ，2026-09-24 以 in-app browser 直接讀取 v2.5.1。
- 本次直接核對「日常用色與來源參考」：閱讀主體 `#111111`、次要文字 `#4A4A4A`、內頁標題 `#000099`、封面標題 `#000066`、資訊焦點 `#28317B`、比較脈絡 `#7F7F7F`。
- 單期排行採 Focus，一個主角藍，其餘比較灰；標題只陳述資料已支持的結果。原型未涉及 Ordered、Categorical 或狀態色，不據此宣稱完整覆蓋。
- 字體依網站既有角色：Arial／Microsoft JhengHei；文件標題 Cambria／PMingLiU，Mac 可退到 PingFang TC／Songti TC。
- 完整簡報為 1280 × 720、邊距 64 px、標題 36 px、正文 24 px、來源 16 px；網頁中的縮略比較可放大。純圖維持 960 × 420；長文件保留連續段落，不套 16:9。
- 本機 `visuals/designs/transglobe-blue-token-system/design-tokens.json` 標記 v2.5.0。只沿用本次已核對的角色，不宣稱整份等同公開 v2.5.1。舊 CHART-READABILITY 的色碼示例未覆蓋網站的新角色。
- 不使用公司 Logo、原始 Office 容器或第三方圖表程式；示意資料及圖表是本次原創，用來確認套用的行為。

## 尚未驗證的上限

本例只有單期四通路比較。SVG 目前保留文字節點，仰賴已安裝字體；跨平台如出現字形漂移，可由資料及生成來源重製輪廓字版，尚未將該路徑視為完成。Office 套件包含 SVG 不等於已證明每個 Office 版本都以 SVG 渲染。
