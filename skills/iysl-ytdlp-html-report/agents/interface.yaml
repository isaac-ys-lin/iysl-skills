interface:
  display_name: "iysl-ytdlp-html-report"
  short_description: "Transcript-first Traditional Chinese video report with verification sidecar."
  default_prompt: "Use $iysl-ytdlp-html-report for one public video: produce a transcript-grounded v2 Traditional Chinese Markdown/HTML report and a separate verification sidecar."
compatibility:
  canonical_format: "agent-skills"
  adapter_targets:
    - "openai"
    - "generic"
  activation:
    mode: "implicit"
    paths: []
  execution:
    context: "inline"
    shell: "bash"
  trust:
    source_tier: "local"
    remote_inline_execution: "forbid"
    remote_metadata_policy: "allow-metadata-only"
  degradation:
    openai: "native-implicit-skill"
    generic: "description-routed"
