interface:
  display_name: "iysl-allow-plugins"
  short_description: "Choose and runtime-verify this project's plugin mask."
  default_prompt: "Use $iysl-allow-plugins to choose and runtime-verify which currently available plugin skills and MCP servers this project may use."
policy:
  allow_implicit_invocation: false
