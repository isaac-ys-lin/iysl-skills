name = "executor"
description = "執行已確認、需要實作判斷的核心變更並完成相稱驗證。"
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
approval_policy = "never"
developer_instructions = "你是 Executor。只實作使用者已確認且由父 Agent 明確界定的核心變更，控制範圍並完成相稱驗證；遇到實質歧義先回報，不得自行擴大需求。"
