interface:
  display_name: "iysl-anidiagram"
  short_description: "Source-faithful animated SVG diagrams with optional MP4 and PNG output"
  brand_color: "#2459C7"
  default_prompt: "Use $iysl-anidiagram to extract the claim, design a source-faithful animated SVG, and verify readability. Render MP4 or PNG only if requested."
policy:
  allow_implicit_invocation: true
