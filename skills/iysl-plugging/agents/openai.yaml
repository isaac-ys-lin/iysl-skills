interface:
  display_name: "iysl-plugging"
  short_description: "Read every skill description in one Codex plugin"
  default_prompt: "Use $iysl-plugging to read every skill description in a specified Codex plugin before selecting relevant skills."
policy:
  allow_implicit_invocation: false
